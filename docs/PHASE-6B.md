# Phase 6B — Constituent Classification Foundation
Implemented classification contracts, cap/sector filtering APIs, visible classification page and provenance-aware database schema.
Seed rows are explicitly `SEED_PENDING_REFRESH` and must not be treated as authoritative production classifications.
Next: Phase 6C — authoritative classification import/refresh and feed validated constituent sets into live Pulse calculations.